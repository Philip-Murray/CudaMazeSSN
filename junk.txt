for (Position ptr = pos.move(-2, 0); maze.inBounds(ptr); ptr.increment(-2, 0)) {
		if (maze.get(ptr) == visited) {
			break;
		}
		moves.push_back(ptr);

		if (singleStepMode) {
			break;
		}
	}
	for (Position ptr = pos.move(0, 2); maze.inBounds(ptr); ptr.increment(0, 2)) {
		if (maze.get(ptr) == visited) {
			break;
		}
		moves.push_back(ptr);

		if (singleStepMode) {
			break;
		}
	}